#include <stdlib.h>
#include <stdio.h>

int main()
{
    int size = 0;
    int *p = 0;
    array = (int*)malloc(sizeof(int));

    if (array == NULL)
    {
        printf("Failed to allocate memory to allocate the memory for the array");
        exit(0);
    }

    for (int i = 0; i < 10; i++)
    {
        printf("Enter a number\n");
        scanf("%d" &array);


    }
    return 0;
}
