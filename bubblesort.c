#include <stddef.h>
#include <stdio.h>

int *sort_ascending(size_t length, int array[length])
{

    return array;
}

int main()
{
    int array[] = {1, 4, 2, 3, 5};

    printf("%d", *sort_ascending(5, array));
}